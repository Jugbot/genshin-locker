{
  "name": "genshin-locker",
  "private": true,
  "productName": "genshin-locker",
  "version": "1.0.0",
  "description": "My Electron application description",
  "main": "packages/main/dist/index.cjs",
  "scripts": {
    "format:write": "npx lerna run format:write",
    "format": "npx lerna run format",
    "lint:write": "npx lerna run lint:write",
    "lint": "npx lerna run lint",
    "check-types": "npx lerna run check-types",
    "build": "npx lerna run build",
    "compile": "yarn build && electron-builder build --config .electron-builder.config.js --dir --config.asar=false",
    "watch": "node watch.mjs"
  },
  "keywords": [],
  "author": {
    "name": "Lucas Pollice",
    "email": "lp1935@nyu.edu"
  },
  "license": "MIT",
  "devDependencies": {
    "cross-env": "7.0.3",
    "lerna": "^6.6.1",
    "electron-builder": "23.6.0",
    "electron": "20.0.0"
  },
  "dependencies": {
    "sharp": "^0.31.2",
    "ffi-napi": "4.0.3",
    "ref-napi": "3.0.3"
  },
  "engines": {
    "node": "^16.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
